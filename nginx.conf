map $http_cf_connecting_ip $client_ip {
    default $http_cf_connecting_ip;
    "" $remote_addr;
}

limit_req_zone $client_ip zone=contact_limit:10m rate=5r/m;
limit_req_zone $client_ip zone=newsletter_limit:10m rate=5r/m;

upstream public_app {
    server public-app:3000;
}

upstream payload_cms {
    server payload:3000;
}

server {
    listen 80;
    listen 8080;
    server_name datasolace.co.uk datasolace.org datasolace.net;
    return 301 https://datasolace.com$request_uri;
}

server {
    listen 80;
    listen 8080;
    server_name cms.datasolace.com;

    client_max_body_size 25m;

    location = / {
        return 302 https://$host/admin;
    }

    location / {
        proxy_pass http://payload_cms;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $client_ip;
        proxy_set_header X-Forwarded-For $client_ip;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_hide_header X-Powered-By;
    }
}

server {
    listen 80 default_server;
    listen 8080;
    server_name datasolace.com;

    client_max_body_size 12m;
    limit_req_status 429;

    error_page 429 = @json_rate_limited;

    location @json_rate_limited {
        default_type application/json;
        add_header Retry-After "900" always;
        add_header X-RateLimit-Remaining "0" always;
        return 429 '{"error":"Too many requests. Please try again later.","retryAfter":900}';
    }

    location ^~ /api/media/file/ {
        proxy_pass http://payload_cms$request_uri;
        proxy_http_version 1.1;
        proxy_set_header Host cms.datasolace.com;
        proxy_set_header X-Real-IP $client_ip;
        proxy_set_header X-Forwarded-For $client_ip;
        proxy_set_header X-Forwarded-Proto https;
        proxy_hide_header X-Powered-By;
        proxy_hide_header Cache-Control;
        add_header Cache-Control "public, max-age=2592000, immutable" always;
        add_header X-Content-Type-Options "nosniff" always;
    }

    location = /api/contact {
        limit_except POST { deny all; }
        limit_req zone=contact_limit burst=5 nodelay;
        proxy_pass http://public_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $client_ip;
        proxy_set_header X-Forwarded-For $client_ip;
        proxy_set_header X-Forwarded-Proto https;
        proxy_hide_header X-Powered-By;
    }

    location = /api/newsletter {
        limit_except POST { deny all; }
        limit_req zone=newsletter_limit burst=5 nodelay;
        proxy_pass http://public_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $client_ip;
        proxy_set_header X-Forwarded-For $client_ip;
        proxy_set_header X-Forwarded-Proto https;
        proxy_hide_header X-Powered-By;
    }

    location / {
        proxy_pass http://public_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $client_ip;
        proxy_set_header X-Forwarded-For $client_ip;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_hide_header X-Powered-By;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        add_header X-Content-Type-Options "nosniff" always;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
}
